# Technical Step-by-Step Guide: BioMonitor Pipeline

This document records the exact technical steps taken to build the infrastructure and pipeline.

## Phase 1: Cloud Infrastructure Setup (GCP)
1. **Project Creation:**
   - Created a dedicated Google Cloud Project.
   - Project ID: biomonitorcapstone

2. **IAM & Service Account (Done):**
   - Service Account Name: biomonitor-runner
   - Roles assigned:
     - Storage Admin (Full control of GCS buckets)
     - BigQuery Admin (Full control of datasets and tables)
     - Viewer (Project-level visibility)
   - Action: JSON Key generated and uploaded as google_credentials.json

3. **Next Technical Tasks:**
   - Enable BigQuery and Cloud Storage APIs.
   - Configure Terraform with the Service Account JSON.
   - Setup Environment Variables.

## Phase 2: Infrastructure as Code (Terraform)
1. **Configuration Files:**
   - Created `terraform/variables.tf` to centralize project parameters (ID, Region, Bucket/Dataset names).
   - Created `terraform/main.tf` defining GCS Bucket and BigQuery Dataset resources.

2. **Environment Challenge & Solution:**
   - Encountered network/permission issues installing Terraform binary directly on Codespace host.
   - **Solution:** Implemented Docker-based Terraform execution to ensure portability and avoid host dependency.

3. **Execution - Init:**
   - Command used: 
     `docker run --rm -v /workspaces/biomonitor-capstone:/workspace -w /workspace/terraform hashicorp/terraform:latest init`
   - Result: **SUCCESS**. Terraform initialized with Google provider v7.28.0.

4. **Git & GitHub Workaround:**
   - Encountered 403 Forbidden error during HTTPS push.
   - **Solution:** Switched to SSH authentication. 
   - Generated ED25519 key, added to GitHub profile, and updated remote URL to `git@github.com:red-moonx/biomonitor-capstone.git`.
   - Result: **SUCCESS**. First push completed.

5. **Refactoring & Filesystem Cleanup:**
   - Removed duplicate/empty `terraform/terraform` folder.
   - Removed residual `terraform.zip` and binary files to maintain repo health.

6. **Execution - Plan:**
   - Command used: 
     `docker run --rm -v /workspaces/biomonitor-capstone:/workspace -w /workspace/terraform hashicorp/terraform:latest plan`
   - Result: **SUCCESS**. Terraform confirmed the creation of 2 resources (Bucket & BQ Dataset).

7. **Execution - Apply:**
   - Command used: 
     `docker run --rm -v /workspaces/biomonitor-capstone:/workspace -w /workspace/terraform hashicorp/terraform:latest apply -auto-approve`
   - Challenge: 400 Error due to uppercase letters in Bucket Name (`DEcapstone`).
   - Fix: Updated `variables.tf` with lowercase bucket name (`decapstone`).
   - Result: **SUCCESS**. 
     - BigQuery Dataset: `biomonitor_data` created.
     - GCS Bucket: `biomonitor-data-lake-decapstone` created.

## Phase 3: Data Ingestion (dlt)
1. **Environment Setup:**
   - Used `uv` for lightning-fast Python dependency management.
   - Initialized project in `ingestion/` folder.
   - Installed `dlt[bigquery]` and `pandas`.

2. **Source Implementation:**
   - Created `gbif_ingestion.py` using Python generators (`yield`) to handle data streams efficiently.
   - Target: GBIF Occurrence API (Mammalia in Spain).

3. **Credential Management:**
   - Configured `ingestion/.dlt/secrets.toml` to point to the existing `google_credentials.json`.
   - Result: Successful authentication with GCP Service Account.

4. **Regional Troubleshooting:**
   - Problem: Pipeline failed because it defaulted to BigQuery location `US`, but our infrastructure was in `EU`.
   - Fix: Created `ingestion/.dlt/config.toml` forcing `location = "EU"`.

5. **Execution & Refinement:**
   - Command: `uv run gbif_ingestion.py`
   - Initial Result: **SUCCESS**. 
   - Regional Adjustment: Pipeline now forced to `location = "EU"` to match infrastructure.

## Phase 4: Scaling & Compliance (Global BioMonitor)
1. **Redefinition of Scope:**
   - Transitioned from "Mammals in Spain" to **"Global Mammal Biodiversity"**.
   - Justification: Reach significant data volume to demonstrate Data Warehouse optimization (Partitioning/Clustering).

2. **Data Lake Compliance (GCS Staging):**
   - Goal: Meet course criteria for "End-to-End Pipeline" (API -> Lake -> Warehouse).
   - Implementation: Configured `dlt` with `staging="filesystem"` pointing to `gs://biomonitor-data-lake-decapstone`.
   - Result: Data is stored as Parquet in GCS before being moved to BigQuery.

3. **Dependency Update:**
   - Added `gcsfs` to handle Google Cloud Storage filesystem operations within Python.

4. **Massive Ingestion (Scale Up):**
   - Implemented API Pagination (looping with 'offset' and 'limit').
   - Successful load of **10,000 global records** of Mammalia.
   - Verification: Confirmed data staging in GCS Parquet files before BigQuery load.
   - Status: **RAW LAYER COMPLETE**. Ready for dbt transformation.

## Phase 5: Transformation (dbt)
1. **Project Configuration:**
   - Created `dbt_transformation` project.
   - Setup `profiles.yml` in `~/.dbt/profiles.yml`.
   -## 19/04/2026 - Sesión de Escalado y Optimización
*   **Logros:** Refactorización total de la ingesta (10 años, equilibrado por meses). Corregido `taxonKey` para Mammalia. Añadido `time.sleep(1)` para evitar bloqueos 429 de la API.
*   **Estado:** Pipeline pausado y listo para arrancar. BigQuery preparado para recibir 1.3M de registros.

## PASOS PARA MAÑANA (A PRIMERA HORA):
1.  **Arrancar Ingesta:** En Airflow, poner el DAG `biomonitor_pipeline` en **UNPAUSE**. Tardará aprox 75 min (podemos ir desayunando).
2.  **Verificar dbt:** Una vez termine la ingesta, dbt correrá automáticamente para crear `fct_biodiversity_sightings`.
3.  **Dashboard:** Conectar BigQuery a Looker Studio y crear los mapas de calor por especie y estados de conservación.

¡Mañana terminamos el proyecto! 🚀🐘✨
   - Target project: `biomonitorcapstone`, Dataset: `biomonitor_dbt`.

2. **Initial Verification:**
   - Command: `uv run dbt debug`
   - Result: **SUCCESS**. Connection established.

3. **Staging Model Implementation (`stg_gbif_occurrences.sql`):**
   - Goal: Clean raw data and rename reserved keywords (like `order`).
   - Strategy: Used CTEs for Renamed, Filtered, and Deduplicated layers.

4. **Debugging & Quality Tests:**
   - **Problem 1 (Duplicates):** 8 records with duplicate `gbif_id`.
     - *Solution:* Implemented `row_number() over (partition by gbif_id order by event_date desc)`.
   - **Problem 2 (Timestamp formatting):** BigQuery failed on ISO intervals like `2026-01-04T07:40Z/2026-01-04T13:50Z`.
     - *Solution:* Used `SAFE_CAST(SPLIT(event_date, '/')[OFFSET(0)] AS TIMESTAMP)`. This extracts only the start date/time of the interval.
   - **Problem 3 (Test Failures - Nulls):** Found 167 records without species name and 7 without country code.
     - *Solution:* Applied strict `WHERE` filtering in the staging model to ensure `species` and `country_code` are not null.
   - **Problem 4 (BigQuery Errors):** SQL error `Function not found: cast; Did you mean safe.last?`.
     - *Solution:* Fixed typo from `safe.cast` to `safe_cast`.

5. **Final Status:**
   - Command: `uv run dbt build`
   - Result: **STAGING GREEN**. All 8 tests (4 unique, 4 not_null) are passing.

## Phase 6: Orchestration & The "First Green DAG"
1. **Airflow Setup:**
   - Implemented Airflow 3 with a symlinked DAG structure.
   - Target: Orchestrate `dlt ingestion` -> `dbt transformation` sequentially.

2. **The "Example failure" Challenge:**
   - Initial run showed failure in dbt. 
   - **Diagnosis:** It wasn't the biodiversity data, but the default dbt "example" models that were failing their own tests. (Failed: `not_null_my_first_dbt_model_id`).
   - **Action:** Deleted the `models/example` folder. 
   - **Result:** Successfully achieved a **"Green DAG"** with 10k records. Proof of concept verified.

## Phase 7: Scaling to the "Massive Decade" (Seasonally Balanced)
1. **Decision to Scale:**
   - Based on the success of Phase 6, we targeted a 10-year historical dataset (2015-2026).
   - **Problem:** GBIF API restricts searches to a 100k "offset" limit. A simple year-by-year loop would be biased towards the beginning of the year.

2. **The "Balanced Multi-Year" Solution:**
   - Refactored `gbif_ingestion.py` to use a nested loop: `Year -> Month -> Offset`.
   - Configuration: 10,000 records per month for 11 years (132 iterations).
   - Expected Output: **~1.3 Million records**, balanced across all seasons.

3. **Performance Optimization:**
   - Anticipating 1.3M records, optimized `fct_biodiversity_sightings` Mart:
     - **Partitioning:** By `occurrence_timestamp` (Day).
     - **Clustering:** By `species` and `country_code`.

4. **Status:** Ready for full-scale production run and Looker Studio visualization.
